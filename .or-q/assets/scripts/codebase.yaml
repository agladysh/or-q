requires:
  - '@or-q/plugin-core'
  - '@or-q/plugin-store'

commands:
  - run: codebase-filenames
  - stream-map:
      - - f: |
            <file path=${ input quote }>
            ${ input file trim }
            </file>
