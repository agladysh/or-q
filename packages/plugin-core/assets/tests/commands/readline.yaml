suite: readline

tests:
  - name: smoke-fail
    argv: readline
    stdin: ''
    exit: 1
    stderr:
      - contains: 'usage: readline'

  - name: smoke-success
    argv: 'readline "Enter name: "'
    stdin: "test user input\n"
    stdout: 'Enter name: test user input'
