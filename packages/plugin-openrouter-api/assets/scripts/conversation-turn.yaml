requires:
  - '@or-q/plugin-openrouter-api'
  - '@or-q/plugin-store'

commands:
  - user:
      - readline: 'User> '
  - save: CONVERSATION

  - assistant:
      - load: CONVERSATION
      - completions
      - jp: 'choices[0].message.content'
      - unquote
  - save: CONVERSATION
