suite: secret

tests:
  - name: smoke
    argv: exec "[equals, [secret, PWD], [shell, pwd]]"
    stdin: 'ignored'
    stdout: 'true'

  - name: smoke-not-found
    argv: secret OR-Q-NO-SUCH-SECRET
    stdin: 'ignored'
    stdout: ''
    stderr: 'secret OR-Q-NO-SUCH-SECRET not found'
    exit: 1
