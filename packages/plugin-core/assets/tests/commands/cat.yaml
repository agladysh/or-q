suite: cat

tests:
  - name: smoke
    argv: cat ./README.md
    stdin: ''
    stdout:
      - contains: '# OR-Q: OpenRouter Query Porcelain Tool with Plugins'
      - contains: 'KISS, YAGNI, DRY, CS'
