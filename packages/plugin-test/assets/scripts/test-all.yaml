description: Runs all available tests

requires:
  - '@or-q/plugin-test'

commands:
  - discover-tests
  - stream-map: run-test-suite tee
  - if-then:
      - - matches: "FAILED\tSUITE"
      - - fail: 'test-all: failing tests detected'
  - echo: 'test-all: all tests passed'
